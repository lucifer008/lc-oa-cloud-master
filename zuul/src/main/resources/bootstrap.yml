spring:
  profiles:
    active: dev
  application:
    name: zuul-service

---

spring:
  profiles: dev
  cloud:
    nacos:
      config:
        group: DEFAULT_GROUP
        name: ${spring.application.name}
        namespace: dev002
        extensionConfigs:
          - data-id: eureka.properties
            group: GLOBAL_GROUP
            refresh: false
      server-addr: 182.92.221.157:9010
---
spring:
  profiles: test
  cloud:
    nacos:
      config:
        group: DEFAULT_GROUP
        name: ${spring.application.name}
        namespace: public
        # 高版本支持
        extensionConfigs:
          - data-id: eureka.properties
            group: GLOBAL_GROUP
            refresh: false
      server-addr: 182.92.221.157:9010
---